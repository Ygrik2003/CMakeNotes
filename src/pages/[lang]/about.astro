---
import { href, localePaths, type Locale } from '@/i18n/ui';

export function getStaticPaths() {
  return localePaths();
}

const lang = Astro.params.lang as Locale;
return Astro.redirect(href(lang));
---
