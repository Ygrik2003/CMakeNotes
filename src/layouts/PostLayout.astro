---
import { formatDate, href, otherLocale, ui, type Locale } from '../i18n/ui';
import { parsePostId, readingTime, type Post } from '../lib/posts';
import BaseLayout from './BaseLayout.astro';
import TableOfContents from '../components/TableOfContents.astro';
import TagList from '../components/TagList.astro';
import type { MarkdownHeading } from 'astro';

interface Props {
  lang: Locale;
  post: Post;
  headings: MarkdownHeading[];
  translationHref?: string;
  translationExists: boolean;
  prev?: Post;
  next?: Post;
}

const { lang, post, headings, translationHref, translationExists, prev, next } = Astro.props;
const copy = ui[lang];
const minutes = readingTime(post.body ?? '', lang);
const other = otherLocale(lang);
---

<BaseLayout lang={lang} title={post.data.title} description={post.data.description} translationHref={translationHref}>
  <article class="mx-auto w-full max-w-3xl px-4 py-10 sm:px-6 lg:max-w-5xl lg:grid lg:grid-cols-[minmax(0,1fr)_12rem] lg:gap-12" data-pagefind-body>
    <div>
      <p class="font-sans text-sm text-[var(--color-muted)]">
        <time datetime={post.data.date.toISOString()}>{formatDate(post.data.date, lang)}</time>
        <span aria-hidden="true"> · </span>
        <span>{copy.readingTime(minutes)}</span>
        {post.data.draft && (
          <>
            <span aria-hidden="true"> · </span>
            <span class="text-xs uppercase tracking-wide">{copy.draft}</span>
          </>
        )}
      </p>
      <h1 class="mt-3 font-sans text-3xl font-medium tracking-tight sm:text-4xl" data-pagefind-meta="title">
        {post.data.title}
      </h1>
      <p class="mt-4 max-w-2xl text-[var(--color-muted)]">{post.data.description}</p>
      <div class="mt-4">
        <TagList lang={lang} tags={post.data.tags} />
      </div>

      {!translationExists && (
        <p class="mt-6 border-l border-[var(--color-line)] pl-4 text-sm text-[var(--color-muted)]">
          {copy.translationMissing}
          {' '}
          <a class="underline underline-offset-2 hover:text-[var(--color-ink)]" href={href(other, 'posts')}>{ui[other].navPosts}</a>
        </p>
      )}

      <div class="prose mt-10 max-w-3xl">
        <slot />
      </div>

      {(prev || next) && (
        <nav class="mt-14 grid gap-6 border-t border-[var(--color-line)] pt-6 sm:grid-cols-2" aria-label={lang === 'ru' ? 'Соседние статьи' : 'Adjacent notes'}>
          {prev ? (
            <a class="hover:text-[var(--color-muted)]" href={href(lang, 'posts', parsePostId(prev.id).slug)}>
              <span class="block text-xs text-[var(--color-muted)]">{copy.prev}</span>
              <span class="mt-1 block text-sm font-medium">{prev.data.title}</span>
            </a>
          ) : <span></span>}
          {next && (
            <a class="text-right hover:text-[var(--color-muted)]" href={href(lang, 'posts', parsePostId(next.id).slug)}>
              <span class="block text-xs text-[var(--color-muted)]">{copy.next}</span>
              <span class="mt-1 block text-sm font-medium">{next.data.title}</span>
            </a>
          )}
        </nav>
      )}
    </div>
    <TableOfContents lang={lang} headings={headings} />
  </article>
</BaseLayout>
